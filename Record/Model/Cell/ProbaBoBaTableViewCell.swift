//
//  ProbaBoBaTableViewCell.swift
//  Record
//
//  Created by Алешка on 30.11.22.
//

import UIKit
import RealmSwift

/*class ProbaBoBaTableViewCell: UITableViewCell {

   
    
    
    
    
    override func awakeFromNib() {
        super.awakeFromNib()
        
        
    }
    
   

    override func setSelected(_ selected: Bool, animated: Bool) {
        super.setSelected(selected, animated: animated)

        // Configure the view for the selected state
    }
    
    func configure(model: ContactMode) {
        
        nameLabelOne.text = model.nameClients
        numberLabel.text = model.phoneClients
        guard let data = model.imageClients, let image = UIImage (data: data) else {return}
        faceImage.image = image
    }

}*/
